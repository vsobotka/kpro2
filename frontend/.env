# Base URL of the Express backend. Used by server-side load functions and form
# actions, which CANNOT use the Vite dev proxy (the proxy only rewrites requests
# coming from the browser). PUBLIC_ so it also works in universal loads that run
# in the browser; Express has CORS configured for this frontend origin.
PUBLIC_BACKEND_URL=http://localhost:8081
